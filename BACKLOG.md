# ClinVision backlog

Everything pending that isn't tracked anywhere else in the repo. Kept in one file, on GitHub, so it survives across sessions instead of living only in chat history. Add to it as things come up. Once an item fully ships, delete it outright rather than leaving a struck-through "DONE" marker behind — the code and git history are the record of what shipped; this file is only for what's still open. An item that's only partly done (one half shipped, the rest gated or deliberately deferred) stays, describing just the remaining work.

## Marketing site

- **2 more "Solutions" segment pages** (raised 2026-09-03, Solo practice shipped the same day; Multi-doctor clinics shipped 2026-09-06) — the nav's "Solutions" hover menu lists all 4 segments; `solo-practice.html` and `multi-doctor-clinics.html` are both built, Polyclinics and Diagnostic centers still sit in that same menu greyed out and unclickable (`.segment-item.disabled`, `onclick="return false;"`) in `index.html`/`contact.html`/`about-us.html`/`solo-practice.html`/`multi-doctor-clinics.html`'s own copy of the menu (all 5 need the same edit, in the same place, every time one of these ships). `multi-doctor-clinics.html` is the more current reference for a page whose content doesn't fit one point per audience: same `.pg-section`/`.tone-*`/`.item-row` shell in `styles.css`, plus its own `.audience-head`/`.point-list` pattern for a Fix section where an audience needs several parallel points, and `.dark-row-group`/`.dark-row-heading` for an Included section grouped by shared theme instead of one item per column. A new `.tone-*` color per segment (red/green/purple/indigo/teal/rose/cyan are all taken now — pick fresh ones for these two, keeping hero+closing-CTA on the shared blue like every segment page does). Each one also needs: real copy for that segment's actual pain points (not reworded copy from an existing segment), a matching illustration in the same house style as `hero.png`/`solo-practice.png`/`multi-doctor-clinics.png`, and its own icon/color/one-liner already drafted in the menu today (🏢 purple "Many specialties, one front desk" / 🧪 orange "Every walk-in, tracked and billed").
- **About us page — real content.** `about-us.html` exists as a placeholder (nav link is live, page just says "still being written"). Needs the real story, mission, values, and team — written to build trust and credibility with a visitor deciding whether to book a demo. Waiting on the actual content, not a design or build blocker.
- **Pricing page** (raised 2026-09-02) — a new `pricing.html`, linked from the marketing nav next to the existing "About us" link (`index.html`, `contact.html`, `about-us.html`, `solo-practice.html` all share the same nav block). Per the existing copy-style rule, pricing lives on its own page, never folded into `index.html`. Scope not decided yet: plan/tier structure, actual rupee figures, monthly vs. annual, whether a free tier or trial exists — none of that is settled, this is a placeholder-page-plus-nav-link scope only, same shape as `about-us.html` today.
- **Founder's note**, on `index.html`, placed after the Final CTA band and before the footer — a short first-person "why we built this" note. Needs the founder's own real story/voice; do not draft this unprompted.
- **Search engine optimization** (raised 2026-09-02) — checked against the actual repo: there is genuinely none of this today, not even the basics. `index.html` (and every other marketing page — `solo-practice.html`, `about-us.html`, `contact.html`) has a `<title>` and nothing else: no `<meta name="description">`, no Open Graph/Twitter card tags (so a shared link on WhatsApp/LinkedIn/etc. shows no preview image or blurb), no `<link rel="canonical">`, no `robots.txt`, no `sitemap.xml`, no structured data (`Organization`/`SoftwareApplication`/`FAQPage` schema — the FAQ section on `index.html` is a real candidate for `FAQPage` schema once this gets built). Only the logged-in product pages (dashboard, reception, billing, etc.) should stay unindexed — `robots.txt` needs to explicitly disallow those. Not scoped yet: the actual title/description copy per marketing page (needs real keyword thinking, not placeholder text), what the Open Graph preview image looks like, and whether this needs Google Search Console set up alongside it to actually monitor indexing once it ships.
- **`contact.html`'s `WHATSAPP_NUMBER` is still a literal placeholder** (`91XXXXXXXXXX`) — replace with the real business number once one exists. The rest of that page (email prominent in the body via its own "Email us" button, not just the footer) shipped 2026-09-03 in the full contact page redesign.

## Product features

- **Helpful error popups instead of a raw failure** (raised 2026-09-02) — think through the actual errors a clinic could hit (a network drop mid-save, a duplicate phone number on signup, a stale/expired session, a permission a role doesn't have, a double-submit on a slow connection) and give each a real popup explaining what happened and what to do about it, instead of whatever generic/raw error surfaces today. Not scoped in detail yet: needs an actual inventory of the error paths that exist across the app today (many already go through `showToast(err.message || '...')` with a generic fallback — worth checking how many of those fallbacks a real user would ever actually understand) before designing what the improved messages should say.

- **Site-wide translation via a real translation API** (raised 2026-08-31) — a language switch that translates every page (reception, doctor, dashboard, billing, settings, everything, not just the two patient-facing screens below), through an established translation API service, scoped so more languages beyond Hindi can be turned on later without re-architecting. This is a bigger thing than it might look: `display.html` (the waiting-room TV board) and `queue.html` (a patient's own live-status link) already ship a full English/Hindi toggle today, but it's bespoke and narrow — a hand-maintained dictionary of fixed UI strings plus a hand-written phonetic Latin-to-Devanagari transliterator for names/addresses (`toDevanagari`/`HI_WORD_DICT`/`phoneticToDevanagari` in both files), built specifically because those two screens are patient-facing and needed real Hindi rather than a rough machine pass. That approach doesn't scale to the rest of the app (reception's booking form, billing, settings, revenue, etc. — dozens of pages of staff-facing UI text) and doesn't extend to a third language without writing an entirely new dictionary by hand each time. Not scoped yet: which API (Google Cloud Translation vs Azure Translator vs DeepL — cost, quality on Indian-language transliteration of names specifically, and whether it can be called from a static frontend with no server layer beyond Supabase Edge Functions), what gets sent to it (never patient names/phone numbers/addresses to an external API without checking that's acceptable), caching translated strings vs. calling live, and whether the existing bespoke display/queue toggle gets replaced by this or stays as-is since it was purpose-built for exactly those two screens.
- **Real WhatsApp/SMS delivery** — `notifications` table already logs message text but nothing actually sends it. Single biggest concrete gap versus competitor products researched so far (medcore.software, medicore.cc).
- **"You're up soon" proximity alert** (raised 2026-08-30) — a push/SMS notification when a patient is within N people of being seen, instead of them having to keep re-checking their own `queue.html` link. Depends on real SMS/WhatsApp delivery existing first (the item above) — this is really "one more message the same pipeline sends," triggered by queue position crossing a threshold rather than only at booking time. N not yet decided.
- **Phone-in IVR queue-status check** — call a number, hear your queue position via text-to-speech. Aimed at patients without a smartphone or app literacy; not something either researched competitor targets directly.
- **Simple no-show risk flagging** from existing visit history.
- **Patient recall reminders** for recurring/chronic patients.
- **Recurring/follow-up appointment booking** (raised 2026-08-30) — book a follow-up in one action from an existing patient/appointment ("same time next week," "in 3 months," "in 6 months") instead of rebuilding the booking from scratch each time. Has to check the target date against both `closed-dates.html` (one-off closures) and `doctor-holidays.html` (that specific doctor's own days away) before offering it, and needs a sensible fallback when the computed date lands on either (nearest open day? ask reception to pick?) — not decided yet.
- **Doctor visit note** (raised 2026-08-30) — a lightweight free-text field per visit (diagnosis/prescription notes) on `doctor.html`. Today the doctor side only broadcasts status ("running late," "on a break"); nothing clinical gets recorded anywhere in the product. Not scoped: who else can read it back (just the same doctor, or reception/admin too), whether it's editable after the visit closes, and how it interacts with the ABDM FHIR bundle builder (`supabase/functions/_shared/fhir-bundle.ts`) — a real diagnosis field would materially improve that bundle's content, currently the thinnest part of it.
- **AI summary for Revenue and Insights pages** — an on-demand summary of the page's data for the selected date range, placed top-right in line with the existing Period selector on `revenue.html` and `trends.html`. Not scoped yet: what triggers generation, what data gets sent, where the call is made from (this is a static/vanilla-JS Supabase app with no server-side function layer today).
- **Clinic accent-color picker** — let each clinic pick a brand color from a fixed, pre-vetted swatch set (not a free picker, to avoid contrast failures). `--accent` in `styles.css` is a single token already threaded sitewide, so this is mostly a token-swap; `--accent-dark` and a couple of hardcoded tints would need a formula per swatch. Deprioritized at the user's request, not rejected.
- **Patient self-service booking/reschedule/cancel + pre-arrival digital consent forms** — explicitly on hold until the current reception/queue flow is proven out; don't build proactively. Needs an assisted/reception fallback whenever it does happen, not an app-only path (a meaningful share of patients, especially elderly ones, aren't comfortable with self-service app flows).
- **Consultation-duration / wait-time estimation, per clinic** — **data collection shipped 2026-09-01** (`consultation_duration_seconds` on `patients`, migration `057`); the estimation/display half is deliberately still not built, per the release gate below. `callNextPatient`/`finishCurrentPatient` — the only two places a patient ever moves from `in_consult` to `done` — now compute `done_at − called_at` and store it on that same row alongside the age/gender/clinic already captured there at intake, so the clock has actually started on accumulating real data. Nothing reads this column back anywhere yet, and nothing should: **explicit release gate, unchanged — don't ship any time-estimate feature (bucketing by age+gender per clinic, averaging, projecting an ETA from queue position) until there's at least 6 months of data from at least 10 clinics.** When that gate is met, the remaining work is: bucket by age+gender **within each clinic separately** (a fast city clinic and a slower rural one shouldn't share one global average), and multiply that clinic's own average consultation time by however many patients are ahead of a given token to produce a per-patient ETA.
- **Platform admin panel for the ClinVision owner** (raised 2026-09-01) — a private page for running ClinVision-the-business, not any one clinic. Two jobs already exist and are currently done by hand via raw SQL in the Supabase editor (`supabase/checks/activate_clinic_subscription.sql`, `product_feedback_check.sql`); this replaces the SQL with an actual page. "Other work" beyond those two is explicitly open — not scoped past what's below yet.

  **The real problem this has to solve first: nothing in this schema knows the difference between "a clinic's own admin" and "the ClinVision founder."** Every `profiles` row is scoped to exactly one clinic via `my_clinic_id()`, and every permission check in the app (`isAdmin()`, `isDoctor()`) is relative to that one clinic — there's no broader "platform staff" concept anywhere to build on. **Decided 2026-09-01**: a new `platform_admins` table (one row per platform admin, starting with just the owner), checked via a shared `is_platform_admin()` function — the same shape `my_clinic_id()` already is for clinics — reused by every admin-only RPC below. Adding a co-founder or employee later is inserting a row, not writing new code.

  **Where it lives**: a new page, name not decided (`admin.html`?), not linked from any nav. Gated the same way every other role-restricted page already is — `Qlinic.requireLogin()` first, then a new `Qlinic.isPlatformAdmin()` check redirecting anyone else away — but that page-level check is UX only, not the actual security boundary: the real protection is `is_platform_admin()` being checked inside every new admin RPC itself, since a page redirect does nothing to stop someone from calling an RPC directly.

  **Content, scoped to what already exists as manual SQL, plus what a solo founder actually needs to run the business (expanded 2026-09-01)**:
  - **Product feedback** — average rating, total count, a list of recent written comments. Reads from a new `admin_get_product_feedback()`-style RPC (security definer, `is_platform_admin()`-gated) rather than a direct table select — `product_feedback` has no select policy for `authenticated` at all (migration 058), and that stays true; this RPC is a deliberate, narrow, checked exception to it, not a loosening of it.
  - **Clinic subscription management** — a list of clinics with their current `subscription_status`/`trial_ends_at`, and a button to flip active/suspended, replacing the manual `update clinics set subscription_status = ...`. Same RPC-gated pattern.
    - **Per-clinic subscription fee** (raised 2026-09-01) — genuinely new, doesn't exist in the schema at all yet: `clinics.subscription_fee_inr int null` (a plain per-clinic override, since different clinics realistically pay different negotiated amounts — an early customer, a discount, a bigger clinic on a different tier — not one global price), editable inline from this same list.
    - **MRR, as the headline number on this page** — `sum(subscription_fee_inr)` across every `active` clinic. Free once the column above exists; the single most useful "how is the business doing" number for a solo founder, and it doesn't exist anywhere today.
    - **Trial pipeline, sorted by soonest-expiring** — every `trialing` clinic with its `trial_ends_at`, ordered so whoever's about to lapse without ever paying surfaces first. Free from data already collected (migration 053); the only thing this page adds is actually looking at it, instead of it silently ticking down unseen.
    - **Recent signups** — clinics ordered by `created_at`, newest first. Free from an existing column; visibility into growth without a SQL query for something as basic as "who signed up this week."
    - **Contact info surfaced inline** — `admin_email`(and phone, if already collected) shown right next to each clinic in this same list, so following up on a trial or a payment doesn't require a separate lookup.
    - **A free-text note per clinic** (raised 2026-09-01) — genuinely new: `clinics.admin_note text null`, editable from this page. A solo-founder CRM-lite need ("negotiated ₹500/mo, early customer," "follow up after Diwali") that has nowhere to live today.
  - **Clinic activity/health signal** (raised 2026-09-01, flagged as a heavier lift than everything above) — a *paying* clinic that's gone quiet (no patients added in N days) is a churn risk `subscription_status` alone can't show — active-but-unpaid and active-but-dormant look identical today. Doesn't exist yet in any form: needs either a cheap per-clinic "last patient added" query computed on page load (no new schema, just a query against `patients` per clinic — fine at current clinic counts, would need re-thinking if this ever has hundreds of clinics), or a real `last_active_at` column updated some other way. Sequenced after the cheaper items above, not blocking them.
  - **Per-clinic patient volume, with a period selector** (raised 2026-09-01) — booked vs. seen counts per clinic, day/week/month/year/custom, sortable by volume ascending or descending. All from data that already exists in `patients` — the exact same period-selector pattern `revenue.html`/`trends.html` already use per-clinic, just run cross-clinic through an `is_platform_admin()`-gated RPC instead of `my_clinic_id()`.
  - **Doctor and staff headcount per clinic** (raised 2026-09-01) — free, from `doctors`/`profiles` already grouped by `clinic_id`. Answers "who's actually set up and using this" at a glance, no new schema.
  - **Contact/enquiry inbox** (raised 2026-09-03) — reads from the new `contact_enquiries` table (migration 061: name, phone, clinic type, clinic name, city, message, status), fed by the redesigned contact.html. Insert-only for anon via RLS, no select policy yet — same `product_feedback`-style "deliberate, narrow, checked exception" pattern above, via a future `admin_get_contact_enquiries()`-style RPC. Until that RPC/page exists, enquiries are only visible via the Supabase dashboard directly.

- **Operational telemetry — know about problems and usage without a clinic having to report them** (raised 2026-09-01, real discussion behind this one) — the honest answer today is: ClinVision has *zero* visibility into whether something is broken for a clinic, or which parts of the product actually get used, unless someone emails or calls to say so. Splitting this by how expensive each piece actually is, cheapest first:

  **Free — already collected, just needs surfacing in the admin panel**: revenue collected per clinic (a genuinely different number from the clinic's own subscription fee — this is money *the clinic* collects from *its patients* through the app, a proxy for how much real volume/value a clinic is actually running through the tool, not what they pay ClinVision), no-show rate per clinic, average consultation duration per clinic (`consultation_duration_seconds`, migration 057 — already being collected for the future patient-facing wait-time feature, but the same number is also a live operational signal for ClinVision itself: an unusually long or erratic average at one clinic is worth a look).

  **New but cheap — the actual answer to "how do we know a button isn't working"**: nothing today catches a JavaScript error anywhere in this app; it just fails silently for whoever hit it. A new `client_errors` table (`clinic_id` nullable — an error can happen before clinic context is even known, `page`, `message`, `stack`, `user_agent`, `created_at`) plus a global `window.onerror`/`unhandledrejection` handler added once to `clinic-data.js` (already loaded on every page), silently posting to a new insert-only, unauthenticated-friendly RPC — fire-and-forget, never interrupts or alerts the person who hit the error. Needs a sensible cap per page-load (a broken page looping on one error could otherwise spam thousands of identical rows) — exact limit not decided. The admin panel would show recent errors grouped by message + page, so a spike or a recurring pattern is visible ("this error happened 40 times across 3 clinics this week") instead of invisible until someone complains.

  **Bigger — needs its own real scoping pass, not fully speced here**: which pages/features actually get used, how often, by which role (admin/staff/doctor), per clinic — "billing.html opened 200 times this month, revenue.html only 12" is exactly the kind of signal that should drive what gets built or fixed next, and today there's no way to know it at all. This needs a dedicated pass on: what counts as a loggable "event" (a page view is cheap and gives most of the value; click-by-click tracking is far more data for uncertain extra insight), how much volume this realistically generates and whether it needs pruning/retention limits, and one thing worth being upfront about rather than quietly deciding alone — this is real behavioral tracking of clinic staff and doctors, not patients, but it's still a clinic's own people being observed, and that's worth disclosing somewhere (a terms-of-service line, at minimum) rather than shipping silently. Not blocking the cheaper items above; sequenced as its own follow-up.
  - **Everything else** — genuinely not scoped yet. Worth revisiting once the above is built and it's clear what else keeps needing a manual SQL query.

  **Not decided**: exact page/file name, and whether this needs its own distinct login or just recognizes the owner's existing clinic-admin account as also being a platform admin (the `platform_admins` row is keyed by `auth.uid()`, so either works — this is about which account the owner actually logs in with day to day, not a technical constraint).
**Considered and rejected:** waitlist auto-fill for cancelled/no-show slots — doesn't fit Indian clinic behavior (patients overwhelmingly arrive late rather than genuinely no-show, so there's rarely a clean "slot freed up" moment).

**Explicitly out of scope for Qlinic:** EHR/FHIR beyond the ABDM work already shipped, lab modules, IPD/OT scheduling, HR/payroll, AI scribe/radiology AI, insurance/TPA claims, international-patient features — all hospital-scale scope that doesn't fit a solo/small-clinic queue-and-billing tool. Pharmacy billing/inventory was on this list too until 2026-09-03 — reconsidered and scoped below; the reasoning (many clinics run their own in-house dispensing counter today via a second, disconnected piece of software) doesn't apply to the others here, so this isn't a general reopening of the whole list.

## Pharmacy billing & inventory

Raised 2026-09-03: many small clinics dispense medicine on-site (a counter behind or next to reception) and today run a completely separate pharmacy package for it — a doctor/owner has to reconcile two disconnected systems for one patient visit. Scoped here, not started. Real new territory for this codebase: everything so far has billed a *service* (a consultation); this bills and tracks *physical goods a clinic owns and can run out of*, which is a different problem shape.

**Competitor research (mybillbook.in's pharmacy product, checked 2026-09-04)** — worth reading against a key difference: myBillBook sells to *standalone retail pharmacies, pharmacy chains, and wholesalers/distributors* as their own primary business; ClinVision's pharmacy feature is for *a clinic's own attached dispensing counter*, serving that clinic's own patients alongside a consultation, not a business selling medicine as its main line. Several of myBillBook's features exist specifically for that different customer and don't fit here — noted below so this isn't just copied wholesale:
  - **Already covered by what's scoped below**, same or equivalent: GST invoicing with HSN codes and item-wise tax breakup (schema section), batch + expiry tracking with FEFO (Milestone B), a pre-loaded medicine database (the ~500-item starter catalog, Milestone A), barcode scanning at checkout (Milestone C), low-stock alerts (Milestone C, called "reorder surfacing" there), purchase/stock-in recording (Milestone B), receipt printing (reuses `billing.html`'s existing layout, no change needed).
  - **Genuinely missing from the scope below, worth adding** — real reporting on the pharmacy data once it exists, not just billing+stock: fast/slow-moving medicine analysis, an expiring-stock-value report (what's sitting on the shelf about to become a write-off, in ₹, not just a count), and profit margin per medicine (purchase price vs. selling price, the actual margin a pharmacy counter runs on — genuinely new information a clinic doesn't have today even informally). Added to Milestone C below, alongside the H1 register — all three are the same "derive a report off data already being captured" shape.
  - **Considered and deliberately not adopted, given the different customer**: e-Way Bill generation (a legal requirement for *transporting* goods above a value threshold across state lines — a clinic dispensing to its own patients at the counter is a point-of-sale retail transaction, not goods movement, so this practically never applies here); a loyalty/rewards program (fits a retail pharmacy chain competing for walk-in customers, not a clinic's own existing patients); Tally export (a whole-app accounting-export feature if it's ever worth building, not pharmacy-specific — would apply to consultation billing too, so it doesn't belong only in this section); multi-godown/warehouse management (ClinVision is single-location per clinic today — see the "not decided yet" note below, this isn't new); wholesaler/distributor-facing bulk sale flows (not this product's customer at all).
  - **Worth a standing note, not an action item yet**: mandatory GST e-invoicing currently kicks in above a turnover threshold (periodically lowered by the government) — unlikely to affect a single clinic's *pharmacy* revenue specifically any time soon, but worth someone checking the current threshold if/when this ships rather than assuming it never applies.
  - **Already inherited for free from the rest of the app, not a new build**: role-based access (admin/staff/doctor roles already exist), multiple staff transacting concurrently (the existing Supabase-backed architecture already supports this), cloud storage/auto-sync (same), and patient-details capture (the existing `patients` table already holds name/phone/age/gender/address — a pharmacy sale just references the same record, not a second customer database).
  - **Overlaps an existing backlog item, not a separate build**: medicine refill reminders (myBillBook's own feature) would ride the same future notification pipeline as this file's existing "Real WhatsApp/SMS delivery" and "Patient recall reminders for recurring/chronic patients" items — once those ship, a refill reminder is one more trigger on that pipeline, not new infrastructure. WhatsApp bill delivery is the same story.

**The one thing this has to get right first: pharmacy billing only replaces the second software if it's the same app, not necessarily the same document.** Originally scoped as one merged invoice (consultation + medicine on the same `invoices` row) — **revised 2026-09-04, no real problem with 2 separate bills after pushback on this**. What actually caused the "2 softwares" pain wasn't 2 printed receipts, it was 2 disconnected systems: separate patient records, separate stock data, manual double entry, no shared view of what one patient paid today. If ClinVision generates *both* bills itself, from the same patient record, in the same session, that pain is gone regardless of whether 1 or 2 documents come out the other end.

  Two separate bills (a consultation invoice + a pharmacy invoice, same shape as `invoices` today, just tagged by type/reused twice) is arguably the *simpler* build too, and matches how a lot of real multi-service Indian clinics/hospitals already bill — pharmacy, lab, and consultation are commonly separate bills even within one facility. It also sidesteps a genuine tax wrinkle worth knowing regardless of which way this goes: a doctor's own consultation is GST-**exempt** healthcare service (Notification 12/2017, Entry 74), while dispensed medicine is a **taxable** goods sale (5/12/18% depending on the drug) — perfectly legal to mix exempt + taxable lines on one invoice (hospitals do it constantly), but keeping them on separate invoices makes each one's tax treatment unambiguous with zero extra logic.

  **Decided 2026-09-04: build 2 separate invoices**, not 1 merged one. Both still come from the same app/patient record.

  **Reversed 2026-09-05: `revenue.html`/Insights do NOT combine the two.** The original plan above called for both invoice types to roll up together into one reporting picture — built that way in the first pharmacy commit, then explicitly un-done once it was live: pharmacy invoices were leaking into Revenue's totals, Insights' charts, reception's own "already billed today" lookup, and the billing-numbering audit (which started reporting false gaps, since consultation and pharmacy invoices each keep their own separate number sequence sharing one table). `getInvoicesForDate`/`getInvoicesForDateRange`/`getOutstandingInvoices`/`get_billing_audit()` now all filter to `invoice_type = 'consultation'` explicitly. Pharmacy's own numbers live in `pharmacy.html`/`manage-medicines.html`'s stock ledger only, for now — a combined view (if wanted later) would need to be built as its own deliberate feature, not an accidental side effect of a shared table.

**Yes, real inventory tracking is required, not optional** (direct answer to "do we have to build inventory management too?") — a billing-only version (type in medicine names and prices, no stock) is a much smaller build, but it doesn't let a doctor actually drop their separate pharmacy software: they'd still need something tracking what's in stock, what's expiring, and what to reorder. That's the whole reason the second piece of software exists today. Skipping inventory ships half the value.

**Software side — new schema, in rough dependency order:**
- `medicines` — the clinic's own catalog: name, generic name, manufacturer, form (tablet/syrup/injection/etc.), strength (e.g. "500mg"), unit of sale (strip/bottle/piece), drug schedule (H/H1/X — none, or a controlled-substance flag; see the legal note below), default MRP, default selling price, GST rate, HSN code. Clinic-entered, not sourced from an external drug database — there's no free authoritative Indian equivalent to the US's NDC, and licensed pharma data feeds (IQVIA and similar) are a real recurring cost this doesn't need to take on to ship a first version. A CSV bulk-import for the initial catalog (rather than one-by-one entry) matters in practice — a real pharmacy counter can carry 200–2000+ SKUs.
- `medicine_batches` — Indian law requires a batch number + expiry date on a pharma sale invoice (Drugs and Cosmetics Rules), so stock can't be one plain quantity column on `medicines`; each batch needs its own quantity-remaining, expiry date, purchase price, and MRP (MRP can legitimately differ batch to batch). Deduction on sale should be FEFO (first-expiry-first-out), not FIFO — the standard pharmacy practice to minimize expired write-offs, and a real behavioral difference from how a generic e-commerce inventory model would default.
- `medicine_purchases` (stock coming in) — supplier name (plain text for v1, not a full supplier-master table — that's real added scope for later if reordering/supplier-comparison ever becomes its own feature), supplier invoice number, date, and line items (medicine, batch, quantity, purchase price) per purchase. Without this, stock only ever decreases in the system and silently diverges from the real shelf within days.
- Pharmacy sale line items — its own invoice (per the decision above), same line-item shape the existing `invoices` pattern already uses, not a new sales/orders table invented from scratch: medicine_id, batch_id (which batch was actually deducted — needed for a recall or an expiry dispute), quantity, unit price, tax. `batch_id` on the line item is what makes FEFO auditable after the fact, not just correct at the moment of sale.
- `stock_adjustments` — manual corrections (breakage, spillage, an expired batch written off, a physical count that doesn't match the system) — real inventory always drifts from a physical shelf count over time; without this there's no honest way to reconcile the two, only unexplained mismatches.
- **Schedule H1 register** (legal requirement, not a nice-to-have): H1 drugs (a defined list — certain antibiotics, psychotropics) must be sold only against a prescription and logged in a register with patient name, prescriber, drug, quantity, and date, retained for a set period. This can be a *derived* report/query off the sale line items above rather than a separate table, but only if `medicines.schedule` is captured accurately from day one — retrofitting it onto historical sales after the fact isn't possible.
- GST invoicing: the pharmacy invoice needs HSN codes and a proper tax breakdown (CGST+SGST or IGST) per line — a genuinely different shape from the consultation invoice's own (mostly GST-exempt) receipt, which is exactly why keeping them as 2 separate invoices (decided above) is the cleaner path rather than one document juggling both tax treatments.

**Hardware side — much lighter than "pharmacy software" might suggest:**
- **Barcode scanner (optional, but the realistic checkout speed win)**: a standard USB or Bluetooth barcode scanner needs zero special integration — these are all "keyboard wedge" devices that just type the scanned digits into whatever text field currently has focus, exactly like a fast typist would. A plain focused `<input>` at checkout is the entire integration; no driver, no browser API, no vendor SDK.
- **Receipt/invoice printing**: no new hardware need — `billing.html` already has a print-only receipt layout sized for A4 (styles.css's own "Billing receipt" block); the pharmacy invoice reuses that same print layout (just its own invoice number/line items/tax breakdown), not a separate thermal-receipt printer or a new print flow.
- **Barcode label printing** (a clinic generating its *own* labels for repackaged/split stock) is a real, separate hardware+software need (a thermal label printer, typically ZPL-driven) — most small pharmacies dispense using the manufacturer's own printed barcode/strip and never need this. Deferred; not part of a first version.
- Nothing here needs new server-side infrastructure — this is schema + frontend on the same static-site-plus-Supabase architecture the rest of the app already uses.

**Suggested milestones** (mirroring how the ABDM work was sequenced — ship the cheap, clearly-valuable slice first):
- **A** — `medicines` catalog + a single stock-quantity column (no batches yet) + dispensed items generating their own pharmacy invoice (separate from the consultation invoice, per the decision above). Gets pharmacy billing/stock working inside the same app fast; deliberately not yet expiry-safe.
  - **Decided 2026-09-04**: every new clinic's catalog ships pre-seeded with a starter list of roughly 500 common Indian medicines (name, common form/strength, a simple internal reference number, a default GST rate) instead of starting from a completely blank list — the clinic edits/adds/removes from there rather than typing 500 rows by hand on day one. This is a content deliverable ClinVision compiles itself, not a paid drug-database license (still true, see above) — worth being upfront that it needs real care in 2 places specifically: **Schedule H/H1/X classification** (legally meaningful — a wrong classification is a compliance issue, not just a data-quality one) and **MRP figures**, which drift over time and by brand/pack size, so the seed list should read to the clinic as an editable starting point, not authoritative live pricing. Building the actual 500-row list is real work for whenever this milestone starts, not something to draft inline in this file.
- **B** — `medicine_batches` + FEFO deduction + `medicine_purchases` (stock replenishment). This is the point real inventory accuracy exists, not just a stock counter that only ever goes down.
- **C** — low-stock/reorder surfacing, the Schedule H1 register report, barcode scanner support at checkout (the input-focus integration above), `stock_adjustments` for reconciliation, and pharmacy reporting (added after checking a competitor's own feature list, 2026-09-04): fast/slow-moving medicines, an expiring-stock-value report, and profit margin per medicine — all derived off sale/purchase data already captured by this point, same shape as the H1 register right next to them.
- **D (later, if ever)** — barcode label printing, supplier-master table + purchase-order workflow, OCR/import of a distributor's own purchase invoice instead of manual entry.

**Not decided yet:**
- Whether a dispensed item still requires an actual `doctor.html`-side prescription first, or reception/the pharmacy counter can sell over-the-counter items without one — likely both need to exist, but which one is built first isn't decided.
- Whether non-H1 medicines get any prescription-linkage at all, or only H1+ schedule drugs do.
- How a multi-location clinic (if that ever exists on this platform) would separate stock per location — not a problem today (every clinic on ClinVision today is single-location), just flagged so Milestone A's schema doesn't accidentally assume single-location forever.
- Pricing/plan implications — whether pharmacy billing is a paid add-on tier or bundled into the existing subscription (ties into the Razorpay subscription-billing item elsewhere in this file, itself not started).

## Rebrand: ClinVision

Name/tagline decided 2026-08-31; the wordmark treatment and the site-wide swap have since shipped. What's left: trademark search, and the hosting/repo-visibility decision below.

**Decided and shipped:**
- Name: **ClinVision** — capital C, capital V anywhere a person reads it as the brand name (logo, headers, hero copy). Lowercase "clinvision" only in technical strings (domain, email, repo/file names).
- Tagline: **"Where clinics run better"** (sentence case, as shipped) — replaces "Time is the one asset nobody gets back" everywhere. Chosen over "Where Best Clinics Begin" (rejected — reads as aimed at clinics *opening*, a small slice of the real buyer). Passes the same standing rule as always: must read the same to owner/doctor/staff/patient, never patient-only.
- Wordmark-led brand mark — no separate icon/logo mark; the eye/lens icon concepts explored 2026-08-31 were dropped in favor of the "ClinVision" wordmark itself (Fraunces, navy ink, gold underline bar) carrying the brand everywhere, including the favicon (a navy rounded-square "C" monogram, not an icon mark).
- Site-wide swap across all 32 pages: "Qlinic" → "ClinVision" in every brand-facing spot (nav/sidebar, auth pages, the waiting-room display board), old tagline replaced, old clock/circle mark removed. The internal `Qlinic.*` JS API namespace was deliberately left untouched — it's code, not brand text.
- Domain `clinvision.in` registered on Namecheap, DNS pointed at GitHub Pages (4 A records + CNAME), WHOIS-verified, redacted in public lookups, live and serving.
- Repo renamed from `qlinic-prototype` to `clinvision`.

**Still open:**
- Trademark: do an exact-match search + get a professional opinion before filing anything — same caution that applied to "Linearr," not yet done for "ClinVision."
- **Hosting decision (2026-08-31):** repo stays **public** for now. GitHub Pages does not serve from a private repository on GitHub's Free plan for a personal account — confirmed the hard way (making the repo private took the live site down; reverted immediately). Plan: upgrade to **GitHub Pro** ($48/year) once there's an active paying clinic — it adds "Pages in private repos," and the deployed site stays public either way, only the source becomes private. A free alternative exists if Pro ever feels like the wrong tradeoff: migrate the deploy target to **Cloudflare Pages** (or Netlify/Vercel), which serves a static site from a private GitHub repo at no cost — not done, since it's real migration work (re-pointing DNS, reconnecting the custom domain), not a toggle.

## ABDM / ABHA integration

Milestones A–C (schema, HFR/HPR capture, FHIR bundle builder, 7 Edge Functions) are built and deployed, running in `ABDM_MOCK=true`. Full technical plan lives in `plans/robust-questing-walrus.md`.

**Milestone D (real sandbox wiring) is blocked entirely on steps outside this codebase**, in order:
1. Register the company legally — current blocker as of 2026-08-28.
2. Register at sandbox.abdm.gov.in for a `clientId`/`clientSecret` (NHA typically replies in 3–4 days).
3. Functional testing against NHA's published test cases, via an empaneled agency (FIME India, Suma Soft, or Tata Communications) — paid.
4. A "Safe-to-Host" security certificate from an STQC/CERT-IN empaneled auditor — paid.
5. NHA final review (administrative, not a code review).
6. Production access. Realistic timeline: 12–20 weeks end to end.

Leaning toward using a third-party ABDM integration partner for the certification process itself rather than doing it in-house — it's a one-time compliance hurdle, not an area where in-house expertise compounds. The Milestone A–C code stays useful either way.

## Qlinic subscription billing (Razorpay)

Scoped 2026-08-30, not started — this is Qlinic charging **clinics** for using Qlinic (the SaaS subscription), a completely different thing from `billing.html`/`billing-consultation.html`, which is a clinic billing **its own patients**. Today `clinics.subscription_status` (migration `053_clinic_subscription_status.sql`) is flipped by hand; this feature is what would make that real.

**The one hard rule this is scoped around: Qlinic's own servers/database must never see or store a raw card number, CVV, or full UPI-linked bank credential.** Every approach below routes actual payment-instrument capture through Razorpay's own hosted Checkout, and Qlinic only ever stores the *token* Razorpay hands back — this is both a PCI-DSS requirement and the only realistic path for a static-frontend app with no PCI-scoped infrastructure of its own.

**How saving a payment method actually works:**
1. Admin clicks "Add payment method" in the new Settings page below.
2. Frontend calls a new Edge Function (`razorpay-create-token-order`) that creates a Razorpay Customer (if one doesn't exist yet for this clinic) and a zero/token-amount order server-side, using the Razorpay secret key — which never reaches the browser.
3. Razorpay's own Checkout.js opens (hosted by Razorpay, not Qlinic) with `save=1`, collects the card or UPI details directly, and returns a `token_id`/payment method reference to the frontend.
4. Frontend hands that token to another Edge Function (`razorpay-save-payment-method`), which verifies it server-side against Razorpay's API and stores only the safe-to-keep parts in a new `clinic_payment_methods` table: Razorpay's `customer_id` and `token_id`, the card network + last 4 digits (or masked UPI VPA), and an `is_default`/`autopay_enabled` flag. No PAN, no CVV, ever, at any point, in Qlinic's own database.
5. Recurring charges (autopay) run entirely on Razorpay's side against that stored token, via Razorpay's Subscriptions API (cards) or UPI Autopay/e-mandate (UPI) — Qlinic's backend only reacts to Razorpay's webhooks (`payment.captured`, `subscription.charged`, `subscription.cancelled`, mandate revoked), which is what actually flips `clinics.subscription_status` automatically instead of by hand.

**New backend pieces needed** (same Supabase Edge Functions pattern already used for ABDM, since this static-frontend app has no other place to put server-side code):
- `razorpay-create-token-order` — creates/reuses the Razorpay Customer, returns an order for Checkout to open against.
- `razorpay-save-payment-method` — verifies the returned token server-side, writes the safe fields to `clinic_payment_methods`.
- `razorpay-webhook` — the one public endpoint Razorpay actually calls; validates Razorpay's webhook signature, updates `clinic_payment_methods.autopay_enabled` / `clinics.subscription_status` accordingly.
- `razorpay-remove-payment-method` — cancels the mandate/token on Razorpay's side first, then deletes the local row — never delete-then-cancel, which would leave an orphaned live mandate still capable of charging the clinic.
- Razorpay's key pair lives in Supabase secrets (`RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET`), same as the ABDM bridge credentials — never a database table.

**New UI, per the user's own direction**: a new card on the Settings hub grid, styled with a gradient (like the login page's own gradient pill) rather than the plain white cards the other settings tiles use — "Subscription & Billing" stands out visually as the one settings item that's actually about money, matching how it's treated with more weight than "Appearance" or "Display screen." Opens a new settings sub-page with:
- The current plan/subscription status (reusing `clinics.subscription_status` — active/trialing/suspended, same states `account-suspended.html` already gates on).
- A payment-method card (the "card for payments" — a visual card UI showing the masked card/UPI info, network logo, "Default" badge) once one exists, or an empty state with "Add payment method" when none does.
- Remove — calls `razorpay-remove-payment-method`, cancels on Razorpay's side first.
- An Autopay toggle — enable/disable recurring auto-charge without removing the saved method entirely.

**Explicitly not scoped/started yet**: any actual Razorpay account setup (business KYC, live API keys), the Edge Functions themselves, the `clinic_payment_methods` migration, or the Settings UI build. This is a scope to build from, not a partial build.
